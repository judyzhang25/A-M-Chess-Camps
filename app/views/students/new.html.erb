<h4 id= "headline" class="center"><br>New Student<br><br></h4><br>

<%= render 'form', locals: {student: @student, family: @family} %>

<% if current_user.role?(:parent) %>
    <%= link_to 'Back', "/home/dash#account" %>
<% else %>
    <%= link_to 'Back', students_path %>
<% end %>