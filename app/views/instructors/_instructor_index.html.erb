<p id="notice"><%= notice %></p>

<% if @instructors.empty? %>
  <h4 id= "headline" class="center"><br>There are no instructors in the system at this time.<br><br></h4><br>
<% else %>
  <h4 id= "headline" class="center"><br>Instructors<br><br></h4><br>

  <table class="highlight">
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Camps</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @instructors.each do |instructor| %>
      <tr>
        <td><%= link_to instructor.name, instructor %></td>
        <td><%= number_to_phone instructor.phone %></td>
        <td><%= instructor.camps.size %></td>
        <td>
          <%= link_to edit_instructor_path(instructor) do %>
            <i class="material-icons">edit</i>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<span class="paginate">
  <%= will_paginate @instructors, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next", :param_name => 'instructors' %>
</span>

    <% end %>